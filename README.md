# yaxunit-test-writer

Claude Code skill для написания юнит-тестов конфигураций 1С:Предприятие на
фреймворке [YAxUnit](https://github.com/YAXUNIT/yaxunit).

Скилл проводит агента через весь цикл написания теста: подготовка тестового
модуля и его регистрация в `Configuration.xml`, проектирование тестов (типы,
именование), реализация given/when/then, изоляция зависимостей через Мокито,
организация тестовых наборов и тегов. Триггерится на фразы вида «напиши тест»,
«покрой тестами», «яксюнит».

## Установка

```
skills add astrizhachuk/yaxunit-test-writer -a claude-code -y
```

Глобально (для всех проектов):

```
skills add astrizhachuk/yaxunit-test-writer -a claude-code -g -y
```

## Обновление

```
skills update
```

Обновляет все скиллы, установленные через `skills add`, включая этот.

## Структура репозитория

Содержимое скилла (`SKILL.md`, `references/`, `templates/`) лежит в подпапке
`yaxunit-test-writer/`, а не в корне репозитория — это обходит баг установщика
`skills`, из-за которого `SKILL.md` в корне репозитория триггерит режим
установки, копирующий только сам файл `SKILL.md` без сопутствующих файлов.
На команду установки (`skills add astrizhachuk/yaxunit-test-writer`) это не
влияет.

## Requirements / Assumptions

Скилл рассчитан на проекты 1С, созданные из шаблона
[`astrizhachuk/bootstrap-1c`](https://github.com/astrizhachuk/bootstrap-1c),
и полагается на его раскладку каталогов:

- `src/` — исходный код конфигурации;
- `test/` — расширение с тестами, `test/CommonModules/` — тестовые модули,
  `test/Configuration.xml` — регистрация;
- `tools/` — вспомогательные утилиты.

Дополнительные допущения:

- Имя расширения с тестами — **`Тесты`** (используется в тексте скилла как
  константа).
- Версия платформы 1С читается из `CLAUDE.md` проекта (строка «Версия
  платформы для тестирования») — по ней выбирается папка `templates/{версия}/`.
- Шаблоны XML-метаданных сейчас есть только для платформы `8.3.16`
  (`templates/8.3.16/`). Для других версий — добавить каталог `templates/{версия}/`
  по аналогии.

## TODO

- Параметризовать имя расширения с тестами (сейчас захардкожено как `Тесты`).
- Шаблоны метаданных для других версий платформы.
